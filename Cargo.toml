# contextual n-gram differencer
[package]
name = "ctx"
version = "0.1.0"
edition = "2024"

[dependencies]
harper-core = "2.5.0"
reqwest = { version = "0.13", features = ["blocking", "json"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
url = "2.5"

